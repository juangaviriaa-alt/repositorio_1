Create database Lugar_Trabajo
Use Lugar_Trabajo
Create table Lugar_Trabajo(
Codigo-Int primary key, // en el Int se guarda el codigo del lugar de trabajo, el cual se le asignara un numero unico a cada lugar de trabajo.
Nombre_Lugar-Varchar, // en el Varchar se guarda el nombre del lugar de trabajo
Precio_adomicilio-Int, // en el Int se guarda el precio a domicilio
Direccion_Int); // en el Int se guarda la direccion del lugar de trabajo, el cual se le asignara un numero unico a cada direccion.

